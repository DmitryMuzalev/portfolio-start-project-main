import styled from "styled-components";

const MenuWrapper = styled.ul`
  display: flex;
  align-items: center;
  gap: 3rem;
`;
const MenuItem = styled.li``;

export const S = { MenuWrapper, MenuItem };
